<?xml version="1.0" ?>

<project name="le" default="ls" >
	<target name="ls" description="executes a ls -l command under unix">
		<var name="directory" value="." />
		<exec executable="ls -l ${directory}" />
	</target>
</project>
