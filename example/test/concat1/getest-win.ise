test

	xconcat1

default

	class ("TEST_[A-Z0-9_]*")
	feature ("test_[a-z0-9_]*")
	prefix ("X")
	testgen (".")
	compile ("(es4 -ace ise.ace & cd EIFGEN\W_code & finish_freezing -silent) > xcompile.log 2>&1")
	execute ("cd EIFGEN\W_code & xconcat1")

cluster

	concat1: "$GOBO/example/test/concat1"

end
