test

	xconcat1

default

	class ("TEST_[A-Z0-9_]*")
	feature ("test_[a-z0-9_]*")
	prefix ("X")
	testgen (".")
	compile ("(vec -no -a:ve.esd & vec -eu -y -no) > xcompile.log 2>&1")
	execute ("xconcat1")

cluster

	concat1: "$GOBO/example/test/concat1"

end
