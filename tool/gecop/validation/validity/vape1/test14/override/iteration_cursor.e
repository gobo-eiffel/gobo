deferred class
	ITERATION_CURSOR [G]

feature {ANY}

	item: G
		deferred
		end

	after: BOOLEAN
		deferred
		end

feature {ITERABLE, UTF_CONVERTER, NATIVE_STRING, REFLECTOR, DEBUG_OUTPUT, AA}

	forth
		deferred
		end

end