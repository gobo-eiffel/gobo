ISE 5.7/6.0 (ISE Eiffel)

	* The Ace files for ISE Eiffel provided in this package are for
	  Eiffel classic. In order to get Ace files configured for Eiffel
	  for .NET, you will need to set the environment variable
	  $GOBO_DOTNET, and then run the following commands:

	    set GOBO_DOTNET=true
	    cd %GOBO%
	    geant clobber
	    geant install

	* The Ace files for ISE Eiffel provided in this package are for
	  Eiffel classic. In order to get Ace files configured for Eiffel
	  for .NET, you will need to set the environment variable
	  $GOBO_DOTNET, and then run the following commands:

	    set GOBO_DOTNET=true
	    cd %GOBO%
	    geant clobber
	    geant install

SE 1.2r7 (SmartEiffel)

	* MA_DECIMAL.hash_code may violate a precondition in INTEGER.infix "+".

	* 'short' crashes when running 'geant test_se' in $GOBO/test/precomp.

SE 2.* (SmartEiffel)

	* This version of the SmartEiffel compiler is not supported anymore.

VE 5.0b build 2508 (Visual Eiffel)

	* The Visual Eiffel compiler is not supported anymore.
