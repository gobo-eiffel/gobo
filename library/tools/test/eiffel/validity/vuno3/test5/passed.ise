-------------------------------------------------------------------------------

Error code: VUNO

f used in the non-object call is not a class feature.

What to do:
	- make sure the non-object call uses a class feature or
	- replace the non-object call with an object call.

Class: AA
Feature: make
Line: 13
      do
->      a := {BB}.f
      end

-------------------------------------------------------------------------------

Error code: VUNO

The non-object call uses a target type with a deferred base class BB.

What to do: Make sure the target type of the non-object call is an effective class type.

Class: AA
Feature: make
Line: 13
      do
->      a := {BB}.f
      end

-------------------------------------------------------------------------------
