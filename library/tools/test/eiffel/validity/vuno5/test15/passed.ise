-------------------------------------------------------------------------------

Error code: VUNO

The non-object call uses a target type [like Current] AA that is not a class type.

What to do: Make sure the target type of the non-object call is an effective class type.

Class: AA
Feature: make
Line: 11
      do
->      print ({like Current}.f)
      end

-------------------------------------------------------------------------------
