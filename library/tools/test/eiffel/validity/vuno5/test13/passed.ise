-------------------------------------------------------------------------------

Error code: VUNO

The non-object call uses a target type with a deferred base class CC.

What to do: Make sure the target type of the non-object call is an effective class type.

Class: AA
Feature: make
Line: 11
      do
->      print ({CC}.f)
      end

-------------------------------------------------------------------------------
